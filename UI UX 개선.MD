영상 재생 화면 UI/UX 요구사항 1. 기본 목표 

영상 재생 화면은 VLC Android의 실제 영상 플레이어 조작 방식과 최대한 유사한 사용자 경험을 제공해야 한다.

목표는 VLC의 외형을 단순히 흉내 내는 것이 아니라, VLC Android 사용자가 별도의 학습 없이 동일한 방식으로 영상을 조작할 수 있도록 하는 것이다.

특히 다음 VLC Android의 조작 체계를 중요하게 구현한다.

좌측 세로 스와이프 → 화면 밝기 조절 우측 세로 스와이프 → 음량 조절 가로 스와이프 → 재생 위치 탐색 중앙 더블탭 → 재생/일시정지 좌측/우측 영역 더블탭 → 일정 시간 앞/뒤로 이동 핀치 줌 → 영상 크기 조절 화면 비율 버튼 → 화면 비율 모드 순환 하단 재생 컨트롤 상단 뒤로가기 Bluetooth/외부 미디어 버튼을 통한 재생 제어 

VLC Android는 실제로 좌우 영역의 수직 스와이프로 밝기와 음량을 조절하고, 가로 스와이프로 seek하는 구조를 사용한다.

2. 기본 재생 화면 

영상 재생 시 영상이 화면의 주요 영역을 차지해야 한다.

재생 중에는 컨트롤 UI가 자동으로 숨겨질 수 있어야 하며, 사용자가 화면을 터치하거나 필요한 조작을 수행하면 컨트롤 UI가 다시 표시되어야 한다.

UI가 표시되지 않은 상태에서는 영상 감상을 방해하는 요소를 최소화한다.

3. 상단 영역 

컨트롤 UI가 표시된 경우 상단에 다음 요소를 제공한다.

뒤로가기 버튼 현재 재생 중인 영상의 파일명 또는 제목 

뒤로가기 버튼은 Android 시스템 뒤로가기와 동일한 목적을 가진다.

뒤로가기를 실행하면 현재 재생 화면을 종료하고 이전 화면, 일반적으로 영상 라이브러리 화면으로 돌아간다.

현재 영상의 마지막 재생 위치는 유지되어야 한다.

4. 하단 재생 컨트롤 

VLC Android와 유사하게 화면 하단에 재생 컨트롤 영역을 제공한다.

최소한 다음 기능을 제공한다.

현재 재생 시간 전체 영상 길이 seek bar 재생/일시정지 이전 영상 다음 영상 화면 비율 변경 기타 플레이어 기능에 접근할 수 있는 옵션 영역 

재생 진행률을 seek bar를 통해 확인할 수 있어야 하며, seek bar를 직접 조작하여 원하는 위치로 이동할 수 있어야 한다.

5. 화면 비율 변경 

화면 비율 변경 기능은 반드시 별도의 버튼으로 제공한다.

사용자가 버튼을 누를 때마다 지원하는 화면 비율/표시 모드가 순환되어야 한다.

VLC Android는 화면 비율 버튼을 반복해서 눌러 다음과 같은 표시 모드를 선택할 수 있다.

기본적으로 다음과 같은 모드를 고려한다.

Best Fit Fit Screen Fill 16:9 4:3 Center 

각 모드의 실제 동작은 다음 VLC Android 동작을 기준으로 한다.

Best Fit 

영상과 화면의 특성을 고려하여 적절한 표시 비율을 선택한다.

Fit Screen 

영상의 비율을 유지하면서 화면에 맞춘다. 화면 비율에 따라 영상 일부가 화면 밖으로 나갈 수 있다.

Fill 

화면을 최대한 채운다. 영상 비율에 따라 왜곡이 발생할 수 있다.

16:9 

영상 표시 영역을 16:9 기준으로 처리한다.

4:3 

영상 표시 영역을 4:3 기준으로 처리한다.

Center 

영상을 화면 중앙에 표시한다.

지원 모드와 구체적인 구현 방법은 개발자가 판단하되, 사용자가 버튼을 눌러 표시 모드를 명확하게 변경할 수 있어야 한다.

VLC Android는 실제로 이러한 화면 비율 기능을 제공하며, 최근 버전에는 추가적인 비율도 존재할 수 있으므로 구현 시 현재 MPV/앱 환경에서 적절한 모드를 정의한다. VLC의 최신 변경 기록에도 새로운 2:1 aspect ratio 추가가 확인된다.

6. 좌측 세로 스와이프 — 밝기 

화면의 왼쪽 영역에서 위/아래로 세로 스와이프하면 화면 밝기를 조절한다.

동작:

왼쪽 영역 ↑ → 밝기 증가 ↓ → 밝기 감소 

VLC Android의 실제 동작을 따른다.

밝기 조절 중에는 현재 밝기 수준을 사용자가 알 수 있도록 시각적인 피드백을 제공한다.

예:

☀ ████████░░ 80% 

구체적인 표시 형태는 구현자가 결정한다.

7. 우측 세로 스와이프 — 음량 

화면의 오른쪽 영역에서 위/아래로 세로 스와이프하면 음량을 조절한다.

동작:

오른쪽 영역 ↑ → 음량 증가 ↓ → 음량 감소 

VLC Android의 실제 Volume Gesture 동작을 따른다.

음량 조절 중에는 현재 음량 수준을 시각적으로 표시한다.

예:

🔊 ███████░░░ 70% 

Android 시스템의 미디어 음량과 정상적으로 연동되어야 한다.

8. 가로 스와이프 — 재생 위치 탐색 

영상 영역에서 좌우 방향으로 스와이프하면 재생 위치를 변경한다.

← 이전 위치 → 이후 위치 

VLC Android처럼 스와이프 거리에 따라 탐색량을 조절할 수 있어야 한다.

탐색 중에는 현재 위치 또는 이동량을 시각적으로 표시한다.

예:

12:31 → 15:42 

또는

+03:11 

정확한 탐색 감도와 표시 방식은 구현자가 결정한다.

9. 더블탭 동작 

VLC Android의 더블탭 동작을 기준으로 한다.

9.1 중앙 더블탭 

화면 중앙을 더블탭하면:

재생 중 → 일시정지 일시정지 → 재생 

으로 전환한다.

9.2 좌측/우측 더블탭 

화면의 좌측 또는 우측 영역을 더블탭하면 해당 방향으로 짧은 시간만큼 seek한다.

기본 동작은 VLC Android와 동일한 방향을 따른다.

좌측 더블탭 → 뒤로 이동 우측 더블탭 → 앞으로 이동 

VLC Android의 설정에는 Double tap to seek가 있으며, 좌우 가장자리의 반복 탭으로 더 많은 시간을 이동할 수 있다. 예를 들어 여러 번 탭하면 seek 시간이 누적될 수 있다.

기본 seek 시간은 일반적인 VLC Android 기본 동작을 기준으로 하되, 구체적인 값은 구현자가 적절하게 결정한다.

10. 핀치 줌 

가능한 경우 두 손가락을 이용한 pinch gesture를 지원한다.

사용자는 두 손가락으로 확대/축소하여 영상 표시 크기를 조절할 수 있어야 한다.

VLC Android에도 scale gesture를 통한 영상 크기 조절 기능이 존재한다.

이 기능이 화면 비율 변경 기능과 충돌하지 않도록 한다.

11. 롱프레스 

영상 영역을 길게 누르는 동작을 지원할 수 있다.

VLC Android에는 롱프레스 시 빠른 재생을 수행하는 Fast Play 동작이 존재한다.

가능하다면 다음과 같은 동작을 제공한다.

롱프레스 → 2배속 재생 손가락을 떼면 → 기존 속도로 복귀 

단, 이 기능은 핵심 기능보다 우선순위가 낮으며 다른 제스처와의 충돌이 발생하지 않는 경우 구현한다.

12. 재생/일시정지 버튼 

하단 컨트롤 영역에 명확한 재생/일시정지 버튼을 제공한다.

현재 상태에 따라 아이콘을 변경한다.

재생 중 → 일시정지 아이콘 일시정지 → 재생 아이콘 

화면 중앙 더블탭 및 Bluetooth 미디어 버튼과 동일한 재생 상태를 공유해야 한다.

13. Bluetooth 및 외부 미디어 버튼 

Bluetooth 이어폰, 헤드셋, 차량 오디오 및 기타 Android 미디어 컨트롤 장치에서 발생하는 미디어 버튼을 지원한다.

특히 재생/일시정지 버튼은 반드시 지원한다.

Bluetooth 재생/일시정지 ↓ 현재 재생 상태 반전 

즉:

재생 중 → 일시정지 일시정지 → 재생 

화면의 재생/일시정지 버튼과 동일한 동작이어야 한다.

VLC Android 역시 오디오 헤드셋 컨트롤을 지원한다.

가능한 경우 다음 버튼도 지원한다.

다음 트랙 → 다음 영상 이전 트랙 → 이전 영상 

단, Android의 미디어 컨트롤 체계와 충돌하지 않는 범위에서 구현한다.

14. 화면 탭 및 컨트롤 자동 숨김 

화면을 한 번 탭하면 플레이어 컨트롤을 표시하거나 숨긴다.

컨트롤이 표시된 상태에서 일정 시간 동안 사용자의 추가 조작이 없으면 자동으로 숨긴다.

단, 다음 동작은 컨트롤 표시 상태와 관계없이 정상적으로 수행되어야 한다.

밝기 스와이프 음량 스와이프 seek 스와이프 더블탭 핀치 줌 Bluetooth 미디어 버튼 15. 재생 위치 및 시간 표시 

하단 영역에 현재 위치와 전체 길이를 표시한다.

예:

12:31 / 35:20 

사용자가 seek 중일 때는 이동 대상 위치를 즉시 확인할 수 있어야 한다.

영상 종료 시 재생 완료 상태가 라이브러리에 정상적으로 기록되어야 한다.

16. 재생 속도 

현재 재생 속도를 확인하고 변경할 수 있어야 한다.

기본 재생 속도는 앱에서 사용자가 설정한 값을 따른다.

예:

1.0x 1.1x 1.2x 1.25x 1.5x 2.0x 

재생 속도 기능은 MPV의 속도 설정과 연동되어야 한다.

17. MPV 영상 표시 위치 

영상의 실제 표시 위치는 MPV 설정에 의해 제어될 수 있어야 한다.

예:

video-align-y=-1 

이 설정을 사용하면 폴더블의 넓은 내부 화면에서 발생하는 불필요한 위아래 여백을 고려하여 영상을 위쪽으로 배치할 수 있어야 한다.

UI 컨트롤의 위치와 MPV 영상의 표시 위치를 서로 독립적으로 처리할 수 있어야 한다.

18. Android 뒤로가기 

Android 시스템 뒤로가기 버튼/제스처를 지원한다.

재생 화면에서 뒤로가기를 실행하면:

영상 재생 화면 ↓ 이전 화면 / 영상 라이브러리 

로 이동한다.

앱 전체가 종료되는 것이 아니라 현재 재생 화면을 종료하는 것이 기본 동작이다.

현재 영상의 재생 위치와 시청 상태는 보존되어야 한다.

상단의 뒤로가기 버튼도 동일한 동작을 수행해야 한다.

19. 재생 화면 잠금 

가능하다면 VLC Android와 같이 재생 화면의 터치 조작을 잠그는 기능을 제공한다.

잠금 상태에서는 다음과 같은 실수 방지 기능이 필요하다.

화면 터치로 인한 재생 중단 방지 실수로 인한 seek 방지 밝기/음량 제스처 방지 기타 플레이어 컨트롤 방지 

잠금을 해제하기 위한 명확한 사용자 동작을 제공한다.

VLC Android에도 Lock 기능이 있으며 잠금 시 영상 플레이어의 컨트롤과 제스처를 비활성화한다.

20. 폴더블 화면 

폴더블 스마트폰을 주요 사용 환경으로 고려한다.

특히 펼친 상태에서 영상의 가로세로 비율 때문에 큰 여백이 발생할 수 있다.

사용자가 MPV의 영상 위치 설정 및 화면 비율 기능을 이용하여 자신의 선호에 맞게 영상 표시 방식을 조정할 수 있어야 한다.

UI는 영상 표시 영역을 침범하지 않으면서 넓은 화면을 효율적으로 사용해야 한다.

21. 제스처 우선순위 및 충돌 방지 

제스처는 다음 기능이 서로 충돌하지 않도록 설계한다.

제스처 기능 왼쪽 세로 스와이프 밝기 오른쪽 세로 스와이프 음량 가로 스와이프 seek 중앙 더블탭 재생/일시정지 좌측 더블탭 뒤로 seek 우측 더블탭 앞으로 seek 핀치 영상 확대/축소 롱프레스 빠른 재생 단일 탭 컨트롤 표시/숨김 

특히 세로 스와이프와 가로 스와이프를 방향성으로 명확하게 구분해야 한다.

더블탭은 단일 탭으로 잘못 인식되지 않아야 한다.

제스처 충돌이 발생할 경우 실제 VLC Android의 사용자 경험을 우선적으로 참고하여 해결한다.

22. 최종 UX 기준 

완성된 재생 화면은 VLC Android 사용자가 다음 조작을 별도의 학습 없이 사용할 수 있는 수준이어야 한다.

왼쪽 세로 스와이프 → 밝기 오른쪽 세로 스와이프 → 음량 가로 스와이프 → 재생 위치 중앙 더블탭 → 재생/일시정지 좌측 더블탭 → 뒤로 seek 우측 더블탭 → 앞으로 seek 핀치 → 영상 확대/축소 화면 비율 버튼 → 화면 비율 모드 변경 하단 seek bar → 원하는 위치로 이동 Bluetooth 재생/일시정지 → 재생/일시정지 Android 뒤로가기 → 라이브러리로 복귀 

이 문서에서는 위 동작을 요구사항으로 정의하며, 이를 구현하기 위한 구체적인 라이브러리, 클래스, 아키텍처, UI 프레임워크, 이벤트 처리 방식 등은 개발자가 결정한다.

VLC Android의 현재 기능을 기준으로 동작을 검증하고, 단순히 VLC의 UI를 추측하여 구현하지 않는다.

